meta {
  name: List Events (by Transaction DID)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/demo-api/events/{{transactionDid}}
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("response is an array", function() {
    expect(res.body).to.be.an("array");
  });
}
