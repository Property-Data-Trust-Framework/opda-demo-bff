meta {
  name: Sprift Material Information
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/demo-api/sprift/material-information/{{uprn}}
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("response has data", function() {
    expect(res.body).to.have.property("data");
  });
}
